import { PhasePlaceholder } from "@/components/phase-placeholder";

export const metadata = { title: "Events — MLA" };

export default function EventsPage() {
  return (
    <PhasePlaceholder
      title="Events"
      description="Institution-scoped events and platform-wide gatherings with RSVPs."
      planned="Phase 4 — event CMS, calendar view, RSVP."
    />
  );
}