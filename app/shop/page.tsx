import { PhasePlaceholder } from "@/components/phase-placeholder";

export const metadata = { title: "Shop — MLA" };

export default function ShopPage() {
  return (
    <PhasePlaceholder
      title="Digital Products Shop"
      description="Ebooks, software, and PDFs with license-protected downloads."
      planned="Phase 5 — Paystack checkout, license keys, device activation limits."
    />
  );
}