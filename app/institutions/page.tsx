import { PhasePlaceholder } from "@/components/phase-placeholder";

export const metadata = { title: "Institutions — MLA" };

export default function InstitutionsPage() {
  return (
    <PhasePlaceholder
      title="Institutions"
      description="Every institution on the platform, in one place."
      planned="Public institution directory with live activity stats."
    />
  );
}