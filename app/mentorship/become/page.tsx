import { PhasePlaceholder } from "@/components/phase-placeholder";

export const metadata = { title: "Become a Mentor — MLA" };

export default function BecomeMentorPage() {
  return (
    <PhasePlaceholder
      title="Become a Mentor"
      description="List yourself as a mentor and start receiving requests."
      planned="Phase 3 — mentor profile, expertise tags, availability."
    />
  );
}