import { PhasePlaceholder } from "@/components/phase-placeholder";

export const metadata = { title: "About — MLA" };

export default function AboutPage() {
  return (
    <PhasePlaceholder
      title="About MLA"
      description="Our mission: AI literacy and leadership for every Nigerian higher institution."
      planned="Mission statement and institution directory land in this phase's shell expansion."
    />
  );
}